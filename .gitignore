# ── Data files (can be large / sensitive) ─────────────────────────────────────
*.rds
transcripts_raw/

# ── Runtime files generated by the scrapers ───────────────────────────────────
checkpoint_page.txt
scrape_errors.txt

# ── R session artifacts ───────────────────────────────────────────────────────
.Rhistory
.Rapp.history
.RData
.Ruserdata

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Claude Code internal files ────────────────────────────────────────────────
.claude/
